import { Checkbox, CheckboxKind } from "./components/checkbox";

function App() {
    return (
        <div className="h-screen bg-gray-800 grid place-content-center">
            <Checkbox onClick={() => {}} />
        </div>
    );
}

export default App;
